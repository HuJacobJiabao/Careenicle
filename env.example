# rename this file as .env.local

# Local PostgreSQL Database Configuration (if using local database)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=jobtracker
DB_PASSWORD=YOUR_PASSWORD
DB_PORT=5432

# Supabase Configuration (if using Supabase - recommended)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: In your Supabase dashboard > Authentication > URL Configuration:
# Add the following redirect URLs:
# - http://localhost:3000/auth/callback (for local development)
# - https://yourdomain.com/auth/callback (for production)

# Database Provider Configuration
# Set to "postgresql" or "supabase" to determine which database options are available
# postgresql: User can choose between Mock + PostgreSQL
# supabase: User can choose between Mock + Supabase (requires authentication)
# When using supabase, users automatically switch to Supabase data when logged in
NEXT_PUBLIC_DATABASE_PROVIDER=supabase


# Google Maps API key for location services and map display
# Get your API key from: https://console.developers.google.com/
# Enable: Maps JavaScript API, Places API, Geocoding API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
